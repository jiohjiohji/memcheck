Feature: $ARGUMENTS

1. Load relevant skill. Plan: list files to create/modify and tests needed.
2. Present plan. Wait for approval.
3. Write failing tests first. Confirm they fail. Then implement until they pass.
4. Run `npm test && npm run lint`. Fix any failures. Summarize what was built.